version: '3'
services:
  devops-tests:
    container_name: tests
    build:
      dockerfile: ./devops/testing/Dockerfile
      context: ../..
    command:
      bash -c "mvn -B -f pom.xml clean package && mvn -B clean verify -DskipTests"